{ // Eliminates the warning on VSCode: "Experimental support for decorators is a feature that is subject to change in a future release. Set the 'experimentalDecorators' option to remove this warning."
  "compilerOptions": {
    //"allowJs": true,
    "experimentalDecorators": true,
    "target": "es5"
  }
}
